# Rules — frontend (scope: frontend) — template

> Rule riêng cho code UI. `dev-frontend` áp dụng khi viết code; `reviewer` enforce.
> `MUST` = ràng buộc cứng (blocking). `SHOULD` = khuyến nghị. **Bảng trống = suy convention từ component/page lân cận như hiện tại.**
> Prefix id: `R-FE-*`. Không bịa rule — chỉ ghi điều dự án thực sự yêu cầu.

| id | severity | rule |
| --- | --- | --- |
<!-- | R-FE-1 | MUST | Dùng component từ design system của dự án, không tự dựng lại control đã có. | -->
<!-- | R-FE-2 | MUST | Mọi text hiển thị đi qua lớp i18n, không hardcode chuỗi. | -->
<!-- | R-FE-3 | SHOULD | State dùng chung quản lý tập trung theo pattern hiện có, tránh prop-drilling sâu. | -->
