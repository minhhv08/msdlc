{
  "name": "msdlc",
  "version": "0.1.0",
  "description": "Stack-agnostic delivery pipeline for Claude Code: spec → architect → [ADR gate] → planner → dev + qc-designer (parallel) → reviewer → qc-executor + security-auditor → chronicler. Project specifics (stack, commands, lockstep contracts) come from .claude/profile.md at runtime, never hardcoded.",
  "author": { "name": "minhhv" },
  "keywords": ["pipeline", "agents", "orchestration", "qc", "architecture", "planner"],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/block-read-secrets.sh\""
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/block-bash-dangerous.sh\""
          }
        ]
      }
    ]
  }
}
